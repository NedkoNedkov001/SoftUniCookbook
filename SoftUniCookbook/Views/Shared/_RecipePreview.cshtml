@using Cookbook.Core.Models
@model RecipePreviewViewModel

<div class="col-12 col-sm-6 col-md-4 d-flex align-items-stretch flex-column">
                        <div class="card bg-light d-flex flex-fill">
@*                            <div class="card-header text-muted border-bottom-0">
                                Digital Strategist
                            </div>
*@                            <div class="card-body pt-0">
                                <div class="row">
                                    <div class="col-7">
                                        <h2 class="lead"><b>@Model.Name</b></h2>
                                        <p class="text-muted text-sm">@Model.Description</p>
                                        <ul class="ml-4 mb-0 fa-ul text-muted">
                                            <li class="small"><span class="fa-li"><i class="fas fa-utensils"></i></span> Serve size: @Model.ServeSize</li>
                                            <li class="small"><span class="fa-li"><i class="fas fa-user"></i></span> Added by: <a href="/">@Model.AuthorNickname</a></li>
                                        </ul>
                                    </div>
                                    <div class="col-5 text-center">
                                        <img src="data:image;base64,@System.Convert.ToBase64String(Model.Picture)" alt="user-avatar" class="img-circle img-fluid">
                                    </div>
                                </div>
                            </div>
                            <div class="card-footer">
                                <div class="text-right">
                                    <a href="#" class="btn btn-sm bg-teal">
                                        <i class="fas fa-bookmark"></i>
                                    </a>
                                    <a href="#" class="btn btn-sm btn-primary">
                                        <i class="fas fa-book-open"></i> View
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>